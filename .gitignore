# dependencies
node_modules
.pnp
.pnp.js

# builds
.next
out
build
*.tsbuildinfo
next-env.d.ts

# env
.env
.env*.local

# vercel
.vercel

# editor / OS
.DS_Store
Thumbs.db
*.swp

# Uncropped PNG originals of the photography (~30 MB). The shipped, optimised
# and watermark-cropped JPEGs live in public/images and ARE committed; these
# masters are kept locally only so the repo stays light for deploys.
assets-source/
